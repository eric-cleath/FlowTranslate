name: FlowTranslate
options:
  bundleIdPrefix: com.flowtranslate
  deploymentTarget:
    macOS: "14.0"
settings:
  base:
    SWIFT_VERSION: "6.0"
    GENERATE_INFOPLIST_FILE: YES
    INFOPLIST_KEY_CFBundleDisplayName: FlowTranslate
    INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.productivity
    MARKETING_VERSION: 0.5.0
    CURRENT_PROJECT_VERSION: 50
targets:
  FlowTranslate:
    type: application
    platform: macOS
    sources:
      - FlowTranslate
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.flowtranslate.app
        PRODUCT_NAME: FlowTranslate
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        ENABLE_HARDENED_RUNTIME: YES
        CODE_SIGN_ENTITLEMENTS: FlowTranslate/FlowTranslate.entitlements
