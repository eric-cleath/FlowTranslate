---
title: PallasOwl Translator（原 FlowTranslate）开发记录与下一版本计划
aliases:
  - PallasOwl Translator 路线图
  - PallasOwl Translator 版本记录
tags:
  - FlowTranslate
  - macOS
  - 翻译
  - AI写作
  - 开发记录
status: active
current_version: 0.11.3
updated: 2026-09-01
---

# PallasOwl Translator（原 FlowTranslate）开发记录与下一版本计划

## 下一版本开发中 — 选中即译焦点行为

- T 图标失去当前选区焦点后自动消失，包括点击其他位置、切换应用或开始键盘操作。
- 点击 T 图标自身时保持有效并继续展开翻译结果。
- 跨语写作外部进度提示改为 PallasOwl 蓝青—紫色视觉风格，使用渐变 T 图标、半透明圆角卡片、流动进度条和淡入淡出动画。
- 实时字幕的原文和译文区域在内容更新时自动向下跟随，旧内容向上滚动，始终显示最新字幕；仍可手动向上滚动查看历史内容。
- 普通打开设置窗口时默认定位到“通用设置”；从文档翻译或实时字幕的专用齿轮进入时，仍直接定位到对应设置页面。

## 0.11.3（2026-09-01）— 选中即译兼容读取修复

- 修复 0.11.2 中部分浏览器、PDF 和第三方 App 的两种“选中即译”方式均不响应的问题。
- 优先使用 macOS 辅助功能读取选区；读取失败时，仅在检测到实际拖动选中或双击选词后执行一次兼容读取。
- 普通左键单击、右键操作不会触发兼容读取，避免影响日常复制和粘贴。
- 恢复剪贴板前会确认内容仍属于本次兼容读取，不覆盖用户后续复制的内容。

## 0.11.2（2026-09-01）— 菜单栏与选中即译稳定性

- 修复焦点位于其他 App 时，从菜单栏选择“设置”可能无法显示设置窗口的问题；打开前主动激活应用并将窗口置前。
- 取消“选中即译”自动模拟 `Command+C` 的剪贴板后备读取，避免干扰右键复制/粘贴及用户执行的 `Command+C`、`Command+V`。
- “选中即译”改为只通过 macOS 辅助功能读取选区；不支持该读取方式的 App 可继续使用原有“划词翻译”快捷键。

## 0.11.1（2026-08-31）— 选中即译视觉优化补充

- T 图标统一使用菜单栏的蓝紫渐变白色 T 图标，并增加描边、阴影、悬停放大和“翻译”提示。
- 翻译结果改为适配深浅色模式的半透明圆角卡片，显示功能名称、语言方向和关闭按钮。
- 译文跟随通用字号与行距；原文默认折叠，可按需展开。
- 卡片底部增加朗读、复制和打开主翻译窗口。
- 增加翻译进度动画、失败提示和重试按钮。
- 长内容在卡片内部滚动；支持点击外部、Esc 或超时关闭，鼠标停留时暂停自动关闭。
- 新选中文字时复用现有卡片并更新内容，避免重复创建结果窗口。

## 0.11.0（2026-08-31）— 词典模式、跨语写作语言与本地统计

- 选中单词或短语时，在线查询 Wiktionary 并显示语言、词性、释义和例句；不使用 AI 补全。
- 跨语写作源语言改为完整语言列表，默认简体中文，支持语言方向互换并保存选择。
- “关于”页面增加各功能模块的本地使用次数，只记录次数，不记录或上传处理内容。
- 通用设置删除重复的“联系我们”，邮箱统一放在“关于”页面。

## 0.10.2（2026-08-31）— 实时字幕持续运行与导出

- 实时字幕运行期间，字幕主界面保持置顶；切换到其他功能模块后，采集和翻译继续运行，并由独立字幕悬浮窗继续显示。
- 实时字幕新增“仅译文 / 原文与译文”输出选择。
- 新增 Obsidian Markdown 与纯文本导出，双语 Markdown 使用 50:50 对照表格。
- 修复离开实时字幕页面会自动停止字幕的问题；字幕仅在用户点击“停止字幕”或退出 App 时结束。
- “选中即译”结果窗口新增关闭按钮、点击外部关闭、Esc 关闭及 20 秒自动消失，避免窗口长期置顶。

## 0.10.1（2026-08-31）— 选中即译兼容性修复

- 修复在浏览器网页、PDF 和部分第三方 App 中选中文字后，“显示 T 图标”与“自动显示结果”均无反应的问题。
- 优先通过 macOS 辅助功能读取选区；目标 App 不提供选区属性时，自动使用与现有划词翻译相同的静默复制兼容方案。
- 静默复制完成后恢复原剪贴板文字，避免影响日常复制内容。

## 下一版本已确认修改

- 打开“AI 文本处理”或切换引擎时，只显示“API Key 已配置/未配置”状态，不立即读取钥匙串中的密钥内容。
- 只在用户点击“编辑密钥”、“显示密钥”、“验证”或实际调用该引擎时读取 API Key，避免仅浏览设置页就弹出钥匙串授权窗口。

## 0.10.0（2026-08-31）— 品牌更名、选中即译与系统体验优化

- App 显示名称改为 **PallasOwl Translator**，同步更新 Finder、窗口、菜单、DMG 和权限说明；Bundle ID 与签名证书保持不变。
- 钥匙串改为按当前功能和引擎需要读取，不再启动时读取全部 API Key；本次运行内缓存已读取密钥。
- API Key 保存改用 `SecItemUpdate` 原位更新，保留钥匙串项目的“始终允许”访问控制。
- 实时字幕开启 Apple Speech 自动标点；译文未返回时不再重复显示原文。
- PallasOwl Translator 主界面可见时隐藏黑色字幕悬浮窗，主界面关闭后再自动显示。
- 设置增加“关于”页，显示图标、版本、简介、联系邮箱、GitHub、更新入口和 MIT License。
- 新增独立的 **选中即译**：鼠标选中其他 App 中的文字后，可显示 T 图标再翻译，或配置为自动显示结果；默认关闭。
- 原有“划词翻译”的快捷键与处理逻辑保持不变。

### 0.10.0 测试重点

- 覆盖安装后检查辅助功能、屏幕与系统音频、麦克风和语音识别权限是否保留。
- 测试启动、文本翻译、AI 文本处理和设置切换时的钥匙串弹窗次数。
- 测试实时原文标点、主窗口与悬浮字幕的自动切换。
- 在 Safari、Chrome、PDF、Word 和文本编辑器中测试“选中即译”两种显示方式。

## 0.9.3（2026-08-31）— 实时字幕授权回调崩溃修复

- 根据 0.9.1 崩溃报告定位到 Swift 6 actor 隔离错误：系统 TCC 在后台队列返回语音识别/麦克风授权结果时，调用了主线程隔离的回调。
- 将语音识别和麦克风授权包装移出 `MainActor`，授权结果可从 TCC 后台队列安全恢复异步任务。
- 保留 0.9.2 中对系统音频、屏幕录制、麦克风和语音识别的用途声明。

### 0.9.3 测试重点

- 首次点击“开始字幕”并回应语音识别或麦克风授权时，App 不应退出。
- 分别测试“麦克风”、“全部应用”和“指定应用”三种声音来源。

## 0.9.2（2026-08-31）— 系统音频启动崩溃修复

- 修复选择“全部应用”或“指定应用”后点击“开始字幕”，PallasOwl 被 macOS 隐私保护机制直接终止的问题。
- Info.plist 补充 `NSScreenCaptureUsageDescription`，向系统说明 ScreenCaptureKit 仅用于捕获应用音频，不保存屏幕画面。
- Info.plist 补充 `NSAudioCaptureUsageDescription`，兼容新版 macOS 对系统音频捕获的独立用途说明。
- 保留麦克风和语音识别用途说明，三类实时字幕权限现在均有明确说明。

### 0.9.2 测试重点

- 选择“全部应用”后开始字幕，应弹出系统权限提示或正常运行，不再直接退出。
- 选择某个指定 App 后开始字幕，验证只接收该 App 的声音。
- 首次授权后若系统要求退出 App，应重新打开 PallasOwl 再开始字幕。

## 0.9.1（2026-08-31）— 实时字幕配置与音频源完善

- 修复进入实时字幕设置后分类栏无法正常切换的问题；设置内容改为可滚动，关闭设置窗口后默认回到文本翻译设置。
- 实时字幕音频源增加“指定应用”，动态列出当前正在运行的普通 App，并允许只捕获所选 App 的声音。
- 菜单栏增加“实时字幕音频源”二级菜单，可直接选择麦克风、全部应用或某个运行中的 App；当前来源显示勾选。
- PallasOwl 自身从应用音频列表和全部应用捕获中排除，避免朗读声音形成循环识别。
- 实时字幕新增完整的独立翻译引擎区域，支持 Apple 系统翻译、DeepL、常见 AI、Ollama 和自定义兼容服务。
- 独立 AI 的 API 地址、Key 和模型单独保存到实时字幕配置；独立 DeepL 使用单独 Key。
- 保留“与文本翻译共用引擎”开关；关闭后使用实时字幕独立引擎，同一时间只启用一个引擎。
- 各实时字幕引擎提供保存和验证操作。
- 全局快捷键新增“打开实时字幕”和“开始/停止实时字幕”，默认分别为 `⌃⌥L`、`⌃⌥R`，均可在设置中修改。

### 0.9.1 测试重点

- 在实时字幕设置与其他设置分类之间反复切换，并关闭、重新打开设置窗口。
- 分别测试麦克风、全部应用以及 Safari、Chrome、播放器等指定 App 音频。
- 测试运行中的 App 启动或退出后，菜单栏与设置中的应用列表变化。
- 测试文本翻译共用引擎和实时字幕独立 Apple、DeepL、AI 引擎。
- 测试两个新增全局快捷键及快捷键冲突提示。
- 权限首次变化后完全退出并重新打开 PallasOwl，再测试系统音频捕获。

## 0.9.0（2026-08-31）— 实时字幕基础版

- 顶部模式栏新增“实时字幕”，与翻译、润色、跨语写作和文档模式相互独立。
- 新增麦克风实时转写，使用 macOS Speech Framework 显示持续更新的识别文字。
- 新增“全部应用”系统音频捕获，使用 ScreenCaptureKit 获取其他 App 的播放声音；PallasOwl 自身声音会被排除。
- 新增实时翻译流程，复用文本翻译中当前启用的 Apple 系统翻译、DeepL 或 AI 引擎。
- 增加语言检测：输入语音与目标语言相同时默认只记录原文，不发起翻译，避免无效调用和 Token 消耗。
- 新增独立“实时字幕”设置页，可配置声音来源、输入/目标语言、字幕内容、悬浮窗口和字幕字号。
- 新增麦克风、语音识别、屏幕与系统音频录制的权限说明、检查入口及启动错误提示。
- 新增可拖动的非激活悬浮字幕窗口，支持所有桌面空间和全屏 App，可显示原文、译文或双语。
- 菜单栏新增“实时字幕…”入口。

### 0.9.0 测试重点

- 首次运行分别验证麦克风、语音识别及屏幕与系统音频录制权限。
- 测试麦克风和“全部应用”两种来源能否持续产生字幕。
- 测试输入语言与目标语言相同时是否只记录、不调用翻译引擎。
- 测试 Apple 系统翻译、DeepL 和 AI 引擎的实时响应与错误恢复。
- 测试字幕浮窗在普通桌面、不同 Space 和全屏视频中的显示与拖动。
- 实时字幕当前使用 macOS 系统语音识别；本地 Whisper、指定单个 App、SRT 导出及字幕历史留待后续版本。

## 0.8.3（2026-08-31）— 润色、全局进度与文档导出修正

- 修复润色提示词中 `Polish` 被模型误解为“波兰语”的歧义；润色严格保持原文语言，不翻译、不增加事实、不改变原意。
- 润色模式隐藏翻译方向和交换按钮，统一显示“保持原文语言”。
- 全局快捷键执行跨语写作时使用非激活全局 HUD，可跨桌面和全屏 App 显示，不抢占当前输入焦点，并保证最低可见时间。
- 正文字号和正文行距同步应用于文档译文及译文总结；标题、按钮和状态栏继续使用系统字号。
- PDF 文本提取增加硬换行整理，合并句子内部的版面换行并保留段落、标题和列表边界。
- 文档翻译增加异常结果检查；遇到空结果、仅返回语言名称或长度明显异常时自动重试一次，仍异常则停止并指出具体段落。
- “原文与译文”Markdown 改用 Obsidian 可渲染的固定布局 HTML 表格，原文和译文各占 50%，并尽可能按段落生成对应行。
- 纯文本双语导出同步保留原文与译文，而不是只输出译文。

## 0.8.2（2026-08-30）— 文档界面与状态反馈优化

- 文档载入信息集中到文件卡片，删除顶部重复文件名、底部“文件已就绪”和重复成功提示。
- 文档引擎信息移至左下角设置入口旁，统一使用“当前服务”口径和主界面布局。
- 文档原文全部删除时同步清空译文、导出内容、进度和错误状态，同时保留已选择文件以便重新提取。
- 文档右侧始终只显示译文；“仅译文 / 原文与译文”仅控制导出内容，双语 Markdown 继续输出左右对照表格。
- 全局快捷键执行跨语写作时，等待浮窗改为状态文字与持续动画的线性进度条。
- 系统语音改为独立选择窗口，支持名称搜索、语言筛选、质量标注和逐项试听，并优先排列高质量语音。

## 0.8.1（2026-08-30）— 语言、模型列表与 Obsidian 导出

- 朗读语音设置增加“试听/停止试听”，示例文本根据所选语音语言自动变化。
- 翻译语言增加葡萄牙语、意大利语、阿拉伯语、俄语、越南语、印尼语和泰语，并同步到文本、跨语写作与文档语言选择。
- 添加引擎取消四个分类子菜单，改为一次展开的三列表格；已添加项目显示勾选并禁用。
- 常见服务增加 Perplexity、Mistral AI 和 Cohere。
- AI 模型改为可输入、可补全的组合框；可使用 API Key 从服务商官方接口刷新当前账户模型并在本机缓存。
- 自动过滤嵌入、语音、图片、审核等不适合翻译的模型；官方接口不可用时仍可手动填写模型。
- 原文全部删除时自动清空结果、译文总结和错误状态。
- 取消主界面右下角“清空、复制结果、翻译”三个按钮；保留区域复制并使用回车键处理。
- 文档“提取文字”整理到左侧原文区底部工具栏，状态与字符数量同栏显示。
- 文档导出增加可见格式选择，默认生成带 YAML Frontmatter 的 Obsidian Markdown，也可选择纯文本。
- “原文与译文”导出为左右对照 Markdown 表格，按段落匹配并处理单元格换行和竖线转义。

### 0.8.1 测试发现的问题（已在 0.8.2 处理）

- 跨语写作在主窗口内处理时已有进度反馈，但通过全局快捷键执行“跨语写作并原位替换”时，鼠标附近仍只显示旋转等待图标，没有线性进度条。
- 0.8.2 虽改为线性进度条，但后台调用时仍无法可靠显示；0.8.3 改用非激活全局 HUD，并支持跨桌面和全屏 App。
- 文档模式中手动删除“提取原文”的全部内容后，右侧仍保留上一次翻译结果。
- 0.8.2 在文档原文变为空时同步清空分段翻译结果、导出内容、进度、完成状态和错误提示；已选择的文件或粘贴图片继续保留。
- 文档右上角“仅译文 / 原文与译文”当前同时改变了右侧结果区域，选择双语后会在结果框中混入“原文、译文”标签和原文内容。
- 0.8.2 分离“界面显示内容”和“导出内容”：右侧始终只显示译文，输出选项仅控制导出文件。
- macOS 系统语音数量较多，当前选择器将不同语言、地区、质量以及趣味音效语音全部平铺，难以查找和连续试听。
- 0.8.2 将语音选择改为独立弹出面板，支持语言筛选、名称搜索、质量标注和逐项试听，并保留“自动选择”。趣味语音折叠和常用收藏留待后续评估。

## 0.8.0（2026-08-30）— 系统翻译、引擎单选与朗读控制

- 新增 Apple 系统翻译，固定显示在文本翻译引擎列表第一行；使用 macOS Translation Framework，无需 API Key。
- 引擎开关统一改为互斥单选：开启一个后，同一功能列表中的其他引擎自动关闭。
- 取消“当前”和“设为当前”两套容易混淆的操作口径，开关本身即表示实际调用引擎。
- 扩展中国与国际常见大模型预设，包括通义千问、Kimi、豆包、智谱 GLM、硅基流动、Claude 与 xAI Grok。
- 添加引擎菜单按系统、中国、国际、本地与兼容服务分类。
- 跨语写作增加线性进度反馈与处理中提示。
- 朗读按钮可在播放过程中切换为“停止”；通用设置可选择本机安装的 macOS 系统语音。
- 通用设置新增官方联系邮箱 `pallasowl2026@gmail.com`，可点击打开系统邮件 App。

## 官方联系邮箱

- PallasOwl 联系邮箱：`pallasowl2026@gmail.com`。
- App 的通用设置显示联系邮箱，并提供点击后打开系统邮件 App 的入口。
- README、开发记录、安装说明和 GitHub Release 说明统一使用该邮箱。
- App 不会自动发送日志、翻译内容、API Key 或其他数据；只有用户主动点击联系入口时才打开邮件编辑窗口。

## 0.8.0 引擎设计（已完成）

- “添加引擎”菜单按“系统翻译 / 中国区 AI / 国际 AI / 本地与自定义”分组，避免所有服务混排。
- 系统翻译：Apple 系统翻译。使用 macOS Translation Framework，按语言包可用性显示状态；未下载语言包时由系统提示下载，不需要 API Key。
- Apple 系统翻译固定显示在文本翻译引擎列表第一行，并在新安装时默认加入列表。
- 旧版本升级后自动把 Apple 系统翻译插入第一行，但不强制改变用户当前选中的 DeepL 或 AI 引擎。
- 中国区常见服务：DeepSeek、阿里云通义千问（Qwen）、月之暗面 Kimi、字节豆包、智谱 GLM、百度文心、腾讯混元、MiniMax、硅基流动。
- 国际常见服务：OpenAI、Anthropic Claude、Google Gemini、xAI Grok、Groq、OpenRouter。
- 本地与扩展：Ollama（本地）、自定义 OpenAI 兼容服务。
- DeepL 继续作为专业翻译服务保留在列表中。
- 菜单中显示的是“服务商/引擎”；具体模型名称在右侧配置界面选择或填写，同一服务可保存多个常用模型作为后续扩展。
- Apple 系统翻译需按系统版本和语言组合检查可用性；不支持的系统或语言组合显示原因并自动回退到其他已启用引擎。
- 取消引擎列表中的“当前”标记和右侧“设为当前”按钮，避免与启用开关形成两套选择状态。
- 引擎开关改为互斥单选：开启一个引擎时，同一功能列表中的其他引擎自动关闭。
- 文本翻译、AI 文本处理和独立文档翻译列表统一采用相同的单选开关行为。
- 允许关闭最后一个引擎；此时执行相关功能会提示先在设置中开启一个引擎，不会自动调用其他服务。
- “与文本翻译共用引擎”和“与 AI 文本处理共用引擎”仍是文档翻译的上层互斥选项；开启共用后，独立文档引擎列表暂时不参与调用。

## 0.8.0 交互改进（已完成）

- 跨语写作处理期间显示明确的进度反馈，包括进度条、处理中状态和完成/失败提示，避免等待过程没有反馈。
- 所有朗读入口增加“停止朗读”；朗读过程中原按钮可切换为停止状态。
- 朗读继续使用 macOS `AVSpeechSynthesizer` 系统语音，不调用在线语音服务。
- 在通用设置中增加“朗读语音”配置：默认自动按语言选择，也可从本机已安装的系统语音中手动选择。
- 支持分别为中文、英语、日语等语言选择语音，并提供试听与恢复自动选择。
- 如果某种语音尚未安装，引导用户在 macOS 的系统语音设置中下载，不随 App 重复打包语音文件。

## 0.7.5（2026-08-29）— 文档原文直接输入

- 文档“提取原文”区域支持直接输入或使用 `Command-V` 粘贴文字。
- 粘贴文字后可直接开始翻译，无需选择文件或执行文字提取。
- 取消“粘贴图片”对 `Command-V` 的全局占用；图片改为点击上方“粘贴图片”按钮导入，避免与文字粘贴冲突。

## 0.7.4（2026-08-29）— 模式切换与译文总结

- 文档翻译新增“与 AI 文本处理共用引擎”开关，与“共用文本翻译引擎”互斥。
- 文档可直接复用 AI 文本处理当前服务的 API 地址、Key 与模型。
- 使用输入、划词、截图翻译快捷键时，即使停留在文档页也会自动返回翻译模式。
- 跨语写作快捷键会自动切换到跨语写作模式。
- 手动切换翻译、润色、跨语写作和文档模式时，清空上一模式的原文、结果、错误与进度。
- 从顶部模式中移除独立“总结”。
- 翻译结果区域新增“总结译文”，直接总结完整译文而不是原文。
- 译文摘要显示在独立区域，不覆盖完整翻译结果；总结使用 AI 文本处理引擎。


## 0.7.3（2026-08-29）— OCR 段落与文档布局优化

- 文档图片 OCR 使用文字位置和行间距智能重建段落。
- 自动合并同一段落中由图片版面宽度造成的多余换行。
- 保留明显段落间距与列表项目换行，并处理英文断词连字符。
- 文档设置齿轮移动到左下角，与普通翻译界面保持一致。
- “提取文字”按钮移动到左侧“提取原文”区域下方。
- 提取区域显示字符数量，开始翻译与导出按钮保留在页面右下角。


## 0.7.2（2026-08-29）— 文档提取与翻译分离

- 文档页面新增文件信息区域：显示粘贴图片预览、上传文件名与载入成功状态。
- “提取文字”和“开始翻译”拆分为两个独立动作。
- 文件载入后仅在点击“提取文字”时进行本地解析或 OCR，不调用翻译 API。
- 提取的原文可检查和手动修改；内容不理想时可以不翻译，避免消耗 Token。
- 只有提取原文不为空时才允许点击“开始翻译”。
- 文档页面新增齿轮入口，可打开文档翻译设置。


## 0.7.1（2026-08-29）— 文档入口与引擎管理调整

- 主界面模式栏新增“文档”，可在同一主窗口切换到独立文档处理页面。
- 保留菜单栏“文档翻译…”入口，便于直接打开独立窗口。
- 文档翻译设置改为与文本翻译一致的 Bob 风格引擎列表。
- 支持通过 `+ / −` 增减 AI 与 DeepL 引擎、独立开关、当前引擎标记、保存和验证。
- 每个文档 AI 引擎分别保存 API 地址、Key 与模型配置。
- 保留“与文本翻译共用引擎”开关。


## 0.7.0（2026-08-29）— 文档翻译首版

- 新增独立“文档翻译”窗口，可从菜单栏进入。
- 支持 PDF、DOC/DOCX、TXT、Markdown、RTF、PNG、JPG、HEIC、TIFF 等常见格式。
- 文本型 PDF 直接提取；扫描型 PDF 与图片通过 Apple Vision 在本地 OCR。
- 长文档按段落自动拆分，逐段翻译并显示解析、OCR 和翻译进度。
- 支持“仅译文”和“原文与译文”，可暂停并导出 TXT/Markdown。
- 支持直接从剪贴板粘贴图片（`⌘V`），无需先保存文件即可 OCR 翻译。
- 新增独立文档引擎设置，可共用文本翻译引擎，或单独选择 AI / DeepL。
- 优化锁屏唤醒后的钥匙串读取：已有密钥时不再强制读取，减少重复密码提示。

> [!note] 首版边界
> 当前导出以内容完整为目标，不保留 Word/PDF 的复杂原始排版；EPUB、高保真双语 PDF 与跨启动断点恢复后续完善。

> [!info] 文档范围
> 本文总结项目从 FlowTranslate 原型到 PallasOwl `0.6.0` 的主要演进，并区分“当前测试重点”和“后续候选功能”。

## 一、项目目标

开发一款仅在 macOS 使用的个人翻译工具，结合：

- Bob 的全局划词、截图、输入翻译和快捷调用体验；
- NextAI Translator 的 AI 润色、总结和跨语写作能力；
- DeepL 与多个 AI 翻译服务；
- 在任意 App 中调用，并支持把跨语写作结果原位替换回原输入位置；
- 第一阶段不开发插件系统，先保证核心功能稳定。

## 二、功能口径

| 功能 | 定义 |
| --- | --- |
| 翻译 | 将原文翻译成指定目标语言，可使用 DeepL 或 AI。 |
| 润色 | 在原文语言不变的情况下，修正语法、措辞和表达。 |
| 跨语写作 | 根据中文意图生成自然的目标语言内容，不要求逐字直译。 |
| 总结 | 提炼输入内容的重点。 |
| 原位替换 | 在其他 App 中选中文字，处理完成后直接用结果替换原选区。 |

## 三、最初需求

- 开发原生 macOS App，并直接生成可安装版本。
- 设置界面参考 Bob 的服务列表与详情布局。
- 翻译与 AI 文本处理使用独立的 AI 配置。
- 支持 AI 翻译和 DeepL 翻译，并可验证配置是否可用。
- 支持输入翻译、划词翻译、截图翻译及全局快捷键。
- 选择一个独立、美观的 App 图标。
- 防止每次快捷键调用都创建新窗口。
- 正确使用设置中选择的翻译服务，不能选择 DeepL 后仍默认调用 AI。
- 输入框按 `Enter` 执行翻译，`Shift + Enter` 换行。
- 暂不开发插件系统。

## 四、版本演进

### 0.1.0：首个可安装原型

- 建立 SwiftUI macOS App 基础工程。
- 完成输入区、结果区和基本语言选择。
- 建立 AI 翻译的基本请求链路。
- 生成首个 Apple Silicon DMG 安装包。

### 0.2.0：核心翻译与设置

- 增加文本翻译设置页。
- 分离 AI 翻译与 DeepL 翻译配置。
- 增加 API Key、API 地址、模型及 DeepL 套餐等设置。
- 增加服务验证功能。
- 增加 App 图标和基础菜单栏入口。
- 开始接入全局快捷键。

### 0.3.0：全局调用能力

- 增加输入翻译、划词翻译和截图翻译快捷键。
- 接入 macOS 辅助功能权限和屏幕录制权限。
- 划词时通过复制选区读取文字。
- 截图时调用系统截图并使用 Vision OCR 提取文字。

### 0.3.1：权限与弹窗修复

- 修复已经授权后仍重复提示辅助功能权限的问题。
- 修复输入翻译快捷键没有弹出窗口的问题。
- 修复截图快捷键只有系统提示音、没有显示翻译窗口的问题。
- 改善快捷键调用失败时的错误提示。

### 0.3.2：自动翻译与单窗口

- 修复划词和截图只能提取文字、不能自动翻译的问题。
- 划词和截图取得内容后自动开始翻译。
- 将主翻译界面改为单例窗口，避免每次调用产生一个新窗口。
- 新调用时复用并激活现有窗口。

### 0.3.3：翻译服务选择修复

- 修复选择 DeepL 后主窗口仍调用 AI 的问题。
- 统一设置页“当前服务”和实际请求服务。
- 服务启用、切换后立即保存。
- 当首选服务不可用时，按已启用服务进行合理回退。

### 0.3.4：划词与截图稳定化

- 移除容易误判的辅助功能预检查流程。
- 改为直接读取选区，并在失败时提供明确说明。
- 稳定划词翻译和截图翻译的快捷键调用。
- 保留翻译历史记录。

### 0.4.0：AI 文本处理与跨语写作

- 工作模式调整为：`翻译 / 润色 / 跨语写作 / 总结`。
- 将“AI 写作”统一改名为“AI 文本处理”。
- 明确润色保持原语言，跨语写作负责中文意图到目标语言表达。
- 增加跨语写作快捷键 `⌃⌥W`。
- 在任意 App 中读取选中文字，处理完成后原位替换。
- 增加 OpenAI、Gemini、DeepSeek、Groq、OpenRouter、Ollama 和自定义 AI。
- 翻译与 AI 文本处理分别保存服务配置。
- 输入区支持 `Enter` 执行、`Shift + Enter` 换行。

### 0.4.1：跨语写作方向与多 AI 设置

- 跨语写作改用独立语言方向，不再跟随普通翻译方向。
- 跨语写作输入语言固定为简体中文，目标语言默认英语并独立保存。
- 跨语写作期间增加鼠标附近的“处理中”浮动提示。
- 将各 AI 翻译服务拆成左侧独立条目。
- 各 AI 服务分别保存 API 地址、模型和 API Key。

### 0.4.2：固定本地签名

- 在开发机 A 创建固定签名证书 `FlowTranslate Local Signing`。
- Bundle ID 固定为 `com.flowtranslate.app`。
- 签名证书有效期至 2036 年 8 月。
- 导出公开证书供使用机 B 安装并信任。
- 私钥仅保存在 A 机器钥匙串。
- 目标是以后在 B 机器直接覆盖更新，不再每次重新添加辅助功能权限。
- 从临时签名切换到固定签名时，B 机器仍需最后重新授权一次。

### 0.4.3：多语言截图 OCR 与弹窗重置

- 截图 OCR 增加日语、繁体中文和韩语识别。
- 保留简体中文和英语识别。
- 启用 Vision 自动语言检测和语言校正。
- 每次划词或截图翻译弹窗前，先清空上一次的原文、结果和错误信息。
- 不删除持久翻译历史。
- 继续使用与 `0.4.2` 相同的固定签名身份。

### 0.5.0：集中体验优化与稳定性更新

- 每个 AI 翻译引擎增加独立启用开关，开关状态分别保存。
- DeepL 保留独立开关；关闭当前服务时自动选择其他已启用服务。
- 快捷键设置改为可编辑，可分别设置 Control、Option、Shift、Command 和字母键。
- 增加快捷键重复检测和“恢复默认”按钮，修改后立即重新注册。
- 主界面左下角增加齿轮按钮，可直接打开设置窗口。
- 设置增加“通用设置”页面。
- 通用设置增加“开机自动启动”，使用 macOS 登录项机制。
- 原文和结果默认字号、行距增大，并可在通用设置中调整。
- 原文和结果下方分别增加本地语音朗读与复制按钮。
- 主窗口处理期间显示进度指示和当前处理状态。
- 跨语写作继续保留鼠标附近的独立处理中浮窗。
- App 从睡眠唤醒后重新加载钥匙串密钥。
- 请求前发现内存密钥为空时自动重新读取钥匙串。
- 区分“没有配置 API Key”和“钥匙串暂时不可访问”。
- 保存设置时不再用临时空值覆盖钥匙串中已有的密钥。
- 继续使用固定证书 `FlowTranslate Local Signing` 签名。

### 0.6.0：PallasOwl 品牌与引擎管理更新

- App 正式更名为 **PallasOwl**，窗口、菜单、安装包和项目显示名称同步更新。
- 保留 Bundle ID `com.flowtranslate.app`、钥匙串服务标识和固定签名身份，延续既有配置与系统权限身份。
- 更换明亮的青蓝、紫色猫头鹰 App 图标；菜单栏使用彩色渐变 `T` 图标。
- 文本翻译服务列表改为 Bob 风格：底部 `+ / −` 添加或移除引擎。
- 点击服务行只查看配置，不再自动启用或切换；“启用”和“当前引擎”成为独立状态。
- 移除服务默认保留 API 地址、模型和钥匙串密钥，重新添加后可继续使用。
- AI 文本处理增加相同的 `+ / −` 独立引擎管理。
- AI 文本处理增加“与文本翻译共用引擎”；如果共用的是 DeepL，会明确提示不支持。
- 原有 `0.5.0` 引擎配置自动迁移到新的服务列表和独立配置文件。
- 监听系统解锁、会话重新激活和睡眠唤醒；钥匙串临时不可访问时保留现有密钥并自动重试。
- 截图 OCR 增加智能段落合并，减少网页换行导致的译文断句。
- 从菜单、快捷键或错误入口打开新翻译任务时，先清空上一次原文和结果。
- 通用设置增加界面语言：跟随系统、简体中文、English、Français、日本語。
- 建立原生本地化资源结构，首批覆盖主窗口、菜单和常用设置文字。

发布文件：`PallasOwl-0.6.0-arm64.dmg`

SHA-256：`208330699dec5852e13337aa5e8fa059262d2d0e1b35af5e736f186daa6df78d`

### 0.6.1：当前引擎状态修复

- 修复升级后服务列表只保留 DeepL，但旧的 Ollama/AI 状态仍被当作当前引擎的问题。
- 当前引擎现在必须同时满足“已添加”和“已启用”。
- 当前引擎无效时，按服务列表顺序自动切换到第一个可用引擎。
- 解决把 Ollama 占位 Key `ollama` 错误发送到 OpenAI 地址而出现 `Incorrect API key provided: ollama` 的问题。

发布文件：`PallasOwl-0.6.1-arm64.dmg`

SHA-256：`8e01d78df7c5bd7fa586140e47a5299daf26432343f3d77a4b828b9e8c045b49`

## 五、当前版本能力

当前版本：`0.6.1`

### 翻译入口

| 入口 | 快捷键 | 状态 |
| --- | --- | --- |
| 输入翻译 | `⌃⌥A` | 已实现 |
| 划词翻译 | `⌃⌥D` | 已实现 |
| 截图翻译 | `⌃⌥S` | 已实现 |
| 跨语写作并原位替换 | `⌃⌥W` | 已实现 |

以上为默认快捷键，`0.5.0` 起均可在设置中修改。

### 翻译与 AI 服务

- DeepL API Free / Pro；
- OpenAI；
- Google Gemini；
- DeepSeek；
- Groq；
- OpenRouter；
- Ollama 本地模型；
- OpenAI Chat Completions 兼容的自定义服务；
- 每个设置界面带服务验证功能；
- API Key 保存在 macOS 钥匙串。
- 每个翻译引擎可独立启用或关闭。

### OCR

- 系统区域截图；
- Vision 本地 OCR；
- 支持简体中文、繁体中文、英语、日语和韩语；
- OCR 完成后自动进入翻译流程。

## 六、0.6.0 测试重点

> [!todo] 当前阶段以测试 `0.6.0` 为主

- 在 A、B 和新 MacBook Pro 上验证覆盖安装后权限是否保留；
- 测试睡眠、锁屏、唤醒及重启后的钥匙串读取；
- 测试各 AI 与 DeepL 的独立开关、当前服务切换和配置保存；
- 测试自定义快捷键冲突、重启后保存及全局调用；
- 测试开机启动注册与取消；
- 测试中、英、日、韩文本朗读；
- 测试跨语写作进度提示和原位替换；
- 测试锁屏后首次翻译是否能自动恢复钥匙串，不再需要手动验证；
- 测试文本翻译与 AI 文本处理的添加、移除、启用和“设为当前”；
- 测试“与文本翻译共用引擎”以及 DeepL 不兼容提示；
- 测试截图 OCR 对英文网页折行、中文段落和列表的合并效果；
- 测试中、英、法、日界面切换与重启后的保存；
- 集中记录 Bug 后再决定下一开发版本范围。

## 七、后续候选功能

> [!note] 候选功能
> 以下功能来自 Bob 和 NextAI Translator 的使用方式分析，尚未全部确认为下一版范围，应按稳定性和实际需求分批实施。

### 功能模式待开发清单（长期规划）

> [!important] 不纳入下一版
> 以下模式先进入待开发列表，以后根据实际使用频率逐项设计和加入；当前版本优先完成已经确认的基础功能。

- **校对**：检查错别字、语法、标点和用词，可直接修正或列出修改建议；
- **改写**：提供正式、简洁、自然、礼貌、商务、学术和社交媒体等表达方式；
- **解释**：解释单词、句子或段落的含义、语法、关键词、使用场景和示例；
- **术语翻译**：维护个人术语表、固定译法和不翻译词，保证长文档术语一致；
- **双语阅读**：原文与译文同步定位、逐段重译、复制和导出；
- **邮件写作**：根据要点生成或回复邮件，支持主题、语气和跨语言输出；
- **文档问答**：对已经提取的文档反复提问，提取观点、日期、金额和结论；
- **字幕模式**：翻译 SRT、VTT，保留时间轴并支持双语字幕；
- **对话翻译**：双向语言、连续对话、大字号显示和自动朗读；
- **OCR 阅读模式**：连续截图、多栏版面、表格和指定区域识别。

#### 语音对话模式设想

- **产品边界**：音频、视频文件及临时录音的语音转文字并入现有文档模式，沿用“导入 → 提取原文 → 检查 → 翻译 → 导出”流程；实时双人语音翻译不并入文档模式；
- 作为顶部独立“对话”模式，支持双方分别设置语言和朗读语音；
- 基础版本采用 Apple Speech Framework 进行语音识别，复用现有 Apple、DeepL 或 AI 翻译引擎，并使用 macOS 系统语音朗读译文；
- 支持按住说话或点击开始/停止、实时显示识别文字、识别后修改、自动翻译和自动朗读；
- 以时间顺序保存双方原文与译文，可复制或导出 Obsidian Markdown；
- 提供关闭自动朗读、清空对话和隐私提示；首次使用需要麦克风及语音识别权限；
- 后续再评估在线 AI 实时语音模式，用于连续上下文对话，但需独立 API 并会持续消耗 Token；
- 此模块属于长期规划，不纳入当前下一版本范围。

### 0.8.1 已确认范围（已完成）

- 朗读语音设置增加“试听”，试听过程中可“停止试听”；
- AI 引擎的“模型”输入框改为可搜索下拉列表；保存 API Key 后可从服务商官方 Models API 刷新当前账户可用模型，并保留手动输入作为兼容与故障回退；
- 模型列表只展示适合文本生成/对话的模型，过滤嵌入、语音、图像等不适用于翻译的模型；模型列表在本机缓存并提供“刷新模型”按钮，接口不可用时不影响已保存模型继续使用；
- 翻译语言增加葡萄牙语、意大利语、阿拉伯语、俄语、越南语、印尼语和泰语，并同步到文本、跨语写作与文档语言列表；
- 添加引擎时取消四级分类子菜单，改为一次展开的多列表格；
- 常见引擎列表补充 Perplexity、Mistral 和 Cohere，由用户自行决定是否添加和启用；
- 原文全部删除时，自动清空结果、译文总结和关联错误状态；
- 取消主界面右下角“清空、复制结果、翻译”三个按钮，保留区域内复制，并使用回车触发翻译；
- 文档页面的“提取文字”移入左侧提取区底部工具栏，左侧显示状态或字符数；
- 文档默认导出 Obsidian 可直接使用的 UTF-8 Markdown，包含 YAML Frontmatter；
- “原文与译文”导出使用左右对照 Markdown 表格，按段落配对并正确处理换行和竖线转义；
- `.txt` 作为可选导出格式保留，默认使用 `.md`。

### 借鉴 Bob

- 轻量悬浮翻译窗口，点击空白处自动消失；
- 静默划词翻译；
- 剪贴板翻译；
- 静默截图 OCR，结果直接进入剪贴板；
- 剪贴板图片 OCR；
- Finder 选图 OCR；
- OCR 智能段落还原；
- 连续截图 OCR；
- 二维码识别；
- 历史记录收藏夹；
- 单词模式，包括音标、词性、释义和朗读；
- 多翻译服务结果对照；
- PopClip、Raycast 和 AppleScript 调用接口。

### 借鉴 NextAI Translator

- AI 流式输出；
- 商务邮件、日常交流、学术表达等场景预设；
- 正式、友好、简洁、专业、礼貌、自然等语气选择；
- 自定义提示词及 `$text`、`$sourceLang`、`$targetLang` 变量；
- 当前服务、模型、处理阶段、耗时和错误状态显示；
- 固定窗口；
- 重新生成，以及“更简洁 / 更正式 / 更自然”；
- 术语表，保持人名、公司名和专业词汇一致；
- 生词本与记忆辅助；
- TXT、Markdown 等文件翻译；
- 普通快速模式与深度思考模式；
- 严谨准确与自然创意之间的生成偏好。

### 文档翻译（候选，未加入 0.6.0）

- PDF、DOCX、TXT、Markdown 和 EPUB；
- 在线引擎或 Ollama 本地模型；
- 大 PDF 按页拆分，再按章节、段落和模型上下文进行语义分段；
- 扫描 PDF OCR、进度、暂停、失败重试和断点续传；
- 纯译文或双语对照导出；
- 独立文档翻译引擎，也可选择与文本翻译共用引擎。

## 八、建议的后续版本范围

`0.5.0` 稳定后，优先级建议为：

1. AI 真正的流式输出；
2. 轻量悬浮翻译窗口；
3. 剪贴板翻译与剪贴板 OCR；
4. Finder 选图 OCR；
5. 历史收藏夹；
6. 场景、语气和自定义提示词；
7. TXT、Markdown 等文件翻译。

## 九、安装与更新约定

- 开发机器：A；
- 日常使用机器：B；
- 固定安装路径：`/Applications/FlowTranslate.app`；
- 固定 Bundle ID：`com.flowtranslate.app`；
- 固定签名：`FlowTranslate Local Signing`；
- B 机器需安装并信任 `FlowTranslate-Local-Signing.cer`；
- 后续更新应退出旧版后直接覆盖 App；
- 不要删除系统权限列表中的 FlowTranslate 条目；
- 不要更改 Bundle ID、App 名称或签名证书。

## 十、相关文件

- 当前安装包：`dist/FlowTranslate-0.5.0-arm64.dmg`
- B 机器公开证书：`dist/FlowTranslate-Local-Signing.cer`
- B 机器首次安装说明：`dist/B机器首次安装说明.txt`

## 十一、参考产品

- [Bob 官方指南](https://bobtranslate.com/guide/)
- [NextAI Translator GitHub](https://github.com/nextai-translator/nextai-translator)
